Configure the following variables by creating a `.env` file to make the application run.
REDIS_PASSWORD=
REDIS_HOST=
REDIS_PORT=
Type `npm run dev` in terminal to start the server once redis config is done
